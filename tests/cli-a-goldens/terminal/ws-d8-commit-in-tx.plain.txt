Analysed 3 routines (3 with bodies, 0 parse-incomplete); 2/2 source units parsed; 0 opaque app(s).

HIGH (2):
  [d8-commit-in-transaction] Commit inside a posting transaction span — HandlePosted calls Commit while reachable from PostSalesDoc, which writes 3 tables. A mid-transaction Commit breaks rollback semantics — if the surrounding operation later fails, the data is left half-written.
    ws:src/subscriber.al:11:9 in D8BadSubscriber :: HandlePosted
    confidence: likely
    fix (low): Remove the Commit, or restructure so the surrounding transaction completes (returns control to its caller) before this code runs.
  [d35-commit-in-event-subscriber] Commit reachable from event subscriber — HandlePosted is an event subscriber that calls Commit directly — the publisher cannot roll back the committed state if its work later fails.
    ws:src/subscriber.al:11:9 in D8BadSubscriber :: HandlePosted
    confidence: likely
    fix (medium): Remove the Commit from the subscriber path. If durable side effects are required, schedule them outside the publisher's transaction (e.g. a job-queue entry written without Commit, processed later).

MEDIUM (1):
  [d3-missing-setloadfields] Missing SetLoadFields before a record retrieval — PostSalesDoc runs Get on Header and then accesses field(s) [no.] — no SetLoadFields was set.
    ws:src/posting.al:8:9 in D8PostingChain :: PostSalesDoc
    confidence: likely
    fix (high): Add SetLoadFields(no.) before the retrieval.

INFO (2):
  [d45-event-transitive-table-exposure] Event subscribers expose table transitively from publisher — Publisher 323c302a83ac644b/cef6be65ffad540c497f12505e92be6971f278eddb0f9cf1180f61e12fe6ef20 dispatches to 1 subscriber(s) that write table 11111111-0000-0000-0000-00000000d80a/table/50100; reach=direct; publisherAlsoWrites=yes; subscriberCoverage=complete
    ws:src/posting.al:19:5 in D8PostingChain :: OnAfterPostSalesDoc
    confidence: likely
    fix (high): Treat 11111111-0000-0000-0000-00000000d80a/table/50100 as part of this event's effect surface for permission/transaction reasoning.
  [d9-transaction-span-summary] Transaction span summary — Transaction ending at HandlePosted's Commit spans 3 routines, writes 3 known table(s), publishes 1 event(s). Consider whether all of this needs to be atomic.
    ws:src/subscriber.al:4:5 in D8BadSubscriber :: HandlePosted
    confidence: possible
    fix (low): If the span includes operations that are logically independent, split them into separate transactions with their own Commit boundaries.

Diagnostics (1):
  [warning/detect] d43-event-ishandled-skip: conditionReferences substrate empty; dispatch-site detection limited
